PORT=4000
# SQLite for local dev — zero setup required.
DATABASE_URL="file:./dev.db"

# To switch to Postgres for production, change the provider in prisma/schema.prisma
# to "postgresql" and set DATABASE_URL to something like:
# DATABASE_URL="postgresql://user:password@host:5432/passkonnect"
